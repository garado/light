[project]
name = "light-phone-cli-tui"
version = "0.6.1"
description = "Unofficial CLI/TUI for the Light Phone"
authors = [{ name = "Alexis Garado", email = "alexisgarado@proton.me" }]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "click",
    "rich_click",
    "rich",
    "light-phone-api==0.6.1",
    "rapidfuzz>=3.14.5",
    "inquirerpy>=0.3.4",
]

[project.optional-dependencies]
tui = ["textual", "pyperclip"]

[project.scripts]
light = "light_cli_tui.cli:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["light_cli_tui"]
