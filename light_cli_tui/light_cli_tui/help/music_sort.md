Sort tracks by artist, title, or manual order. Run with no arguments to show the
current sort mode.

**Examples:**

`light music sort` (show current sort mode)

`light music sort artist --desc`

`light music sort title`

`light music sort none` (manual order)
