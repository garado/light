[tool.uv.workspace]
members = ["light_api", "light_cli_tui", "light_daemon"]

[tool.uv.sources]
light_api = { workspace = true }
light-phone-api = { workspace = true }
light-phone-daemon = { workspace = true }

[dependency-groups]
dev = [
    "pytest",
    "pytest-cov",
    "respx",
    "jsonschema",
    "grpcio-tools>=1.83.1",
]
