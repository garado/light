[tool.uv.workspace]
members = ["light_client", "light_api", "light_cli_tui"]

[tool.uv.sources]
light_client = { workspace = true }
light_api = { workspace = true }

[dependency-groups]
dev = [
    "pytest",
    "pytest-cov",
    "respx",
]
